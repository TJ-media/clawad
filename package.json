{
  "name": "clawad",
  "version": "0.1.0",
  "private": true,
  "description": "AI 코딩 에이전트 대기 시간 상태줄 광고 + 개발자 수익 배분",
  "scripts": {
    "lint": "node --check client/statusline.js && node --check client/sync.js && node --check server/index.js",
    "test": "node --test",
    "server": "node server/index.js"
  }
}
